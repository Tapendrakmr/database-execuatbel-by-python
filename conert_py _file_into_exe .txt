1.)First install library pyinstaller (pip install pyinstaller)

2.)pyinstaller --onefile --windowed filename.py      {onefile create single file but there format comes in command to obtain graphical we used windowed}
